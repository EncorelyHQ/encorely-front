/** Valores enviados como enteros en request body (api.md). */
export enum AuthProvider {
  Spotify = 0,
  Google = 1,
  Custom = 2,
}

export enum SwipeDirection {
  Left = 0,
  Right = 1,
  Down = 2,
}

export enum ConcertMood {
  Moshpit = 0,
  Chill = 1,
  VIP = 2,
}
